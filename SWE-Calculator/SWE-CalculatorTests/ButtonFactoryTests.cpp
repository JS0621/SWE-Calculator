#include "ButtonFactoryTests.h"
#include "../SWE-Calculator/ButtonFactory.h"

wxButton* ButtonFactoryTests::CreateButtonID100(wxWindow* window, int id, std::string label, wxPoint point, wxSize size)
{
	return factory->CreateButton(window, id, label, point, size);
}

wxButton* ButtonFactoryTests::CreateButtonID150(wxWindow* window, int id, std::string label, wxPoint point, wxSize size)
{
	return factory->CreateButton(window, id, label, point, size);
}

wxButton* ButtonFactoryTests::CreateButtonID200(wxWindow* window, int id, std::string label, wxPoint point, wxSize size)
{
	return factory->CreateButton(window, id, label, point, size);
}

wxButton* ButtonFactoryTests::CreateButtonID250(wxWindow* window, int id, std::string label, wxPoint point, wxSize size)
{
	return factory->CreateButton(window, id, label, point, size);
}

wxButton* ButtonFactoryTests::CreateButtonID300(wxWindow* window, int id, std::string label, wxPoint point, wxSize size)
{
	return factory->CreateButton(window, id, label, point, size);
}

wxButton* ButtonFactoryTests::CreateButtonLabel1000(wxWindow* window, int id, std::string label, wxPoint point, wxSize size)
{
	return factory->CreateButton(window, id, label, point, size);
}

wxButton* ButtonFactoryTests::CreateButtonLabel2000(wxWindow* window, int id, std::string label, wxPoint point, wxSize size)
{
	return factory->CreateButton(window, id, label, point, size);
}

wxButton* ButtonFactoryTests::CreateButtonLabel3000(wxWindow* window, int id, std::string label, wxPoint point, wxSize size)
{
	return factory->CreateButton(window, id, label, point, size);
}

wxButton* ButtonFactoryTests::CreateButtonLabel4000(wxWindow* window, int id, std::string label, wxPoint point, wxSize size)
{
	return factory->CreateButton(window, id, label, point, size);
}

wxButton* ButtonFactoryTests::CreateButtonLabel5000(wxWindow* window, int id, std::string label, wxPoint point, wxSize size)
{
	return factory->CreateButton(window, id, label, point, size);
}
